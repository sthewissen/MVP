<?xml version="1.0" encoding="UTF-8"?>
<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:icons="clr-namespace:MVP.Styles"
                       xmlns:local="clr-namespace:MVP"
                       xmlns:pages="clr-namespace:MVP.Pages"
                       xmlns:xct="clr-namespace:Xamarin.CommunityToolkit.Extensions;assembly=Xamarin.CommunityToolkit"
                       xmlns:vm="clr-namespace:MVP.ViewModels"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       x:Class="MVP.Pages.ContributionsPage"
                       x:DataType="vm:ContributionsViewModel"
                       x:Name="page"
                       x:TypeArguments="vm:ContributionsViewModel"
                       xmlns:state="clr-namespace:Xamarin.CommunityToolkit.UI.Views;assembly=Xamarin.CommunityToolkit">

    <local:AppFrame IsSecondaryButtonIcon="True"
                    SecondaryButtonText="{x:Static icons:Icons.plus}"
                    ShowSecondaryButton="True"
                    Title="{xct:Translate contributions_title}"
                    x:Name="appFrame">
        <local:AppFrame.Content>
            <Grid state:StateLayout.CurrentState="{Binding State}"
                  state:StateLayout.AnimateStateChanges="False">

                <state:StateLayout.StateViews>
                    <state:StateView StateKey="Loading" RepeatCount="3" RepeatTemplate="{StaticResource contribution_item_loading}" />
                </state:StateLayout.StateViews>

                <CollectionView ItemsSource="{Binding Contributions}"
                            ItemsUpdatingScrollMode="KeepScrollOffset"
                            ItemTemplate="{StaticResource contribution_item}"
                            Scrolled="CollectionView_Scrolled"
                            SelectionMode="None"
                            x:Name="collectionView">

                    <CollectionView.Footer>
                        <BoxView HeightRequest="40"/>
                    </CollectionView.Footer>

                    <!--No search, the API just isn't suited for it. Yet. Maybe one day.
                    <CollectionView.Header>
                        <StackLayout Padding="16,16,16,24">
                            <Frame Style="{StaticResource search_frame}"
                                   touch:TouchEff.Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ContributionsViewModel}}, Path=SearchContributionCommand}">
                                <Grid ColumnDefinitions="*,48"
                                      Padding="24,0"
                                      VerticalOptions="Center">
                                    <Label Style="{StaticResource search_text}"
                                           Text="{xct:Translate search_placeholder}"/>
                                    <Label Grid.Column="1"
                                           Style="{StaticResource search_icon}"
                                           Text="{x:Static icons:Icons.search}"/>
                                </Grid>
                            </Frame>
                        </StackLayout>
                    </CollectionView.Header>
                    -->

                </CollectionView>
            </Grid>
        </local:AppFrame.Content>
    </local:AppFrame>
</pages:BaseContentPage>
